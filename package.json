{
  "name": "gesture-music-app",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest",
    "deploy": "npm run build && npx gh-pages -d dist -m \"Deploy to GitHub Pages\""
  },
  "dependencies": {
    "@mediapipe/tasks-vision": "^0.10.18",
    "tone": "^14.7.77"
  },
  "devDependencies": {
    "gh-pages": "^6.3.0",
    "vite": "^6.0.0",
    "vitest": "^2.0.0"
  }
}
